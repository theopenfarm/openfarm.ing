-- qb:generated
-- @generated by `buddy migrate:regenerate` - edits will be overwritten
CREATE TABLE IF NOT EXISTS "herd_moves" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "farm_id" INTEGER not null REFERENCES "farms"("id"),
  "herd_id" INTEGER not null REFERENCES "herds"("id"),
  "mission_id" INTEGER REFERENCES "missions"("id"),
  "from_field_id" INTEGER not null REFERENCES "fields"("id"),
  "to_field_id" INTEGER not null REFERENCES "fields"("id"),
  "reason" TEXT CHECK ("reason" IN ('rotation', 'gather', 'hazard', 'fence_breach', 'weather', 'manual')) not null default 'rotation',
  "status" TEXT CHECK ("status" IN ('planned', 'authorised', 'flying', 'complete', 'aborted', 'expired', 'rejected')) default 'planned',
  "corridor" TEXT,
  "exclusions" TEXT,
  "min_standoff_m" INTEGER default 25,
  "max_mob_speed_ms" INTEGER default 1.6,
  "max_drive_minutes" INTEGER default 20,
  "rest_after_minutes" INTEGER default 120,
  "peak_mob_speed_ms" INTEGER default 0,
  "drive_minutes" INTEGER default 0,
  "closest_approach_m" INTEGER default 0,
  "abort_reason" TEXT,
  "head_before" INTEGER default 0,
  "head_after" INTEGER default 0,
  "head_stragglers" INTEGER default 0,
  "authorised_by" INTEGER,
  "authorised_at" TEXT,
  "expires_at" TEXT,
  "started_at" TEXT,
  "completed_at" TEXT,
  "summary" TEXT,
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE UNIQUE INDEX IF NOT EXISTS "herd_moves_uuid_unique" ON "herd_moves" ("uuid");

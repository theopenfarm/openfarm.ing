<script server>
const title = 'You are on the list - Open Farming'
const description = 'You will get the field notes as each window opens. A few times a season, nothing else.'
const activeNav = ''
// No card of its own: this page is a confirmation, not a share target.
const ogCard = 'home'
const pagePath = '/subscribed'
</script>

@layout('layouts/site')

@section('content')

<section class="mx-auto pb-24 pt-20 px-5 md:pt-28 max-w-[1240px]">
  <div class="max-w-[52ch]">
    <h1 class="leading-[1.08] text-[2.1rem] sm:text-[2.6rem]">{t:subscribed.title}</h1>
    <p class="mt-5 leading-relaxed text-[1.075rem] text-[var(--text-2)]">
      {t:subscribed.body}
    </p>

    <div class="flex flex-wrap gap-3 mt-8">
      <a href="/field-report" class="btn btn-primary">{t:cta.readReport}</a>
      <a href="/contact" class="btn btn-ghost">{t:cta.book}</a>
    </div>
  </div>
</section>

@endsection

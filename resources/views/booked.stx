<script server>
const title = 'Booked - Open Farming'
const description = 'Your field visit is booked. Someone will call within two working days to agree which block to walk first.'
const activeNav = 'contact'
const pagePath = '/booked'
const subscribeSource = 'booked'
</script>

@layout('layouts/site')

@section('content')

<section class="mx-auto pb-24 pt-20 px-5 md:pt-28 max-w-[1240px]">
  <div class="max-w-[52ch]">
    <h1 class="leading-[1.08] text-[2.1rem] sm:text-[2.6rem]">{t:booked.title}</h1>
    <p class="mt-5 leading-relaxed text-[1.075rem] text-[var(--text-2)]">
      {t:booked.body}
    </p>

    <div class="flex flex-wrap gap-3 mt-8">
      <a href="/field-report" class="btn btn-primary">{t:cta.readReport}</a>
      <a href="/features" class="btn btn-ghost">{t:cta.allCapabilities}</a>
    </div>

    <div class="mt-14 pt-8 border-[var(--line)] border-t">
      <h2 class="text-[1.15rem]">{t:booked.whileYouWait}</h2>
      <p class="mt-2 max-w-[46ch] leading-relaxed text-[0.95rem] text-[var(--text-2)]">
        {t:subscribe.body}
      </p>
      @include('partials/subscribe.stx')
    </div>
  </div>
</section>

@endsection

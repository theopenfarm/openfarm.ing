<script server>
const nodePath = require('node:path')
const catalog = await import(nodePath.join(process.cwd(), 'app/Support/catalog.ts'))

const title = 'Book a field visit - Open Farming'
const description = 'Tell us the crop and the hectares. We walk the block, agree a flight calendar around your season, and you get the first report before committing to anything.'
const activeNav = 'contact'
const pagePath = '/contact'

// The segment select is built from the catalog so the options stay in step
// with the use cases, and the reply can be specific rather than generic.
const useCases = await catalog.allUseCases()
</script>

@layout('layouts/site')

@section('content')

<section class="max-w-[1240px] mx-auto px-5 pt-14 md:pt-20 pb-20">
  <div class="grid gap-12 lg:grid-cols-[1fr_1.05fr] lg:gap-16">
    <div>
      <h1 class="text-[2.1rem] leading-[1.07] sm:text-[2.5rem] lg:text-[2.9rem] max-w-[16ch]">
        Bring us one field and we will fly it
      </h1>
      <p class="text-[1.075rem] text-[var(--text-2)] leading-relaxed mt-5 max-w-[48ch]">
        We walk the block with you, agree what is worth looking for, and fly it. You read the
        report before anyone talks about a calendar.
      </p>

      <ol class="mt-9 list-none p-0 border-l-2 border-[var(--line)]">
        <li class="relative pl-7 pb-7">
          <span class="absolute left-[-0.4rem] top-[0.4rem] w-3 h-3 rounded-[6px] bg-[var(--accent)]"></span>
          <p class="text-[0.97rem] font-semibold">We call within two working days</p>
          <p class="text-[0.92rem] text-[var(--text-2)] leading-relaxed mt-1">Enough to understand the rotation and whether we can fly where you are.</p>
        </li>
        <li class="relative pl-7 pb-7">
          <span class="absolute left-[-0.4rem] top-[0.4rem] w-3 h-3 rounded-[6px] bg-[var(--accent)]"></span>
          <p class="text-[0.97rem] font-semibold">We walk one block with you</p>
          <p class="text-[0.92rem] text-[var(--text-2)] leading-relaxed mt-1">Pick the field that has been annoying you. That is the one worth flying first.</p>
        </li>
        <li class="relative pl-7">
          <span class="absolute left-[-0.4rem] top-[0.4rem] w-3 h-3 rounded-[6px] bg-[var(--accent)]"></span>
          <p class="text-[0.97rem] font-semibold">You get the report</p>
          <p class="text-[0.92rem] text-[var(--text-2)] leading-relaxed mt-1">Problem areas, recommendations, and a prescription if the capability produces one.</p>
        </li>
      </ol>

      <p class="text-[0.9rem] text-[var(--text-2)] leading-relaxed mt-10 max-w-[44ch]">
        Would rather write to a person? <a href="mailto:hello@openfarm.ing" class="text-[var(--accent)] font-semibold">hello@openfarm.ing</a>
      </p>
    </div>

    <div class="rounded-[10px] border border-[var(--line)] bg-[var(--panel)] p-7 md:p-9">
      <form id="demo-form" method="post" action="/api/demo-requests" novalidate>
        <div class="grid gap-5">
          <div class="grid gap-2">
            <label for="name" class="text-[0.88rem] font-semibold">Your name</label>
            <input id="name" name="name" type="text" required autocomplete="name"
              class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)]">
            <p class="text-[0.78rem] text-[var(--text-3)]">So we know who we are calling.</p>
          </div>

          <div class="grid gap-2">
            <label for="email" class="text-[0.88rem] font-semibold">Email</label>
            <input id="email" name="email" type="email" required autocomplete="email"
              class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)]">
          </div>

          <div class="grid gap-2">
            <label for="farm_name" class="text-[0.88rem] font-semibold">Holding</label>
            <input id="farm_name" name="farm_name" type="text" autocomplete="organization"
              class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)]">
          </div>

          <div class="grid gap-5 sm:grid-cols-2">
            <div class="grid gap-2">
              <label for="segment" class="text-[0.88rem] font-semibold">Closest to your operation</label>
              <select id="segment" name="segment"
                class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)]">
                @foreach (useCases as u)
                  <option value="{{ u.slug }}">{{ u.name }}</option>
                @endforeach
                <option value="other">Something else</option>
              </select>
            </div>

            <div class="grid gap-2">
              <label for="hectares" class="text-[0.88rem] font-semibold">Hectares</label>
              <input id="hectares" name="hectares" type="number" min="0" step="1" inputmode="numeric"
                class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)]">
              <p class="text-[0.78rem] text-[var(--text-3)]">Roughly is fine.</p>
            </div>
          </div>

          <div class="grid gap-2">
            <label for="message" class="text-[0.88rem] font-semibold">What is the field that annoys you</label>
            <textarea id="message" name="message" rows="4"
              class="w-full rounded-[10px] border border-[var(--line)] bg-[var(--bg)] px-3.5 py-2.5 text-[0.95rem] text-[var(--text)] resize-y"></textarea>
          </div>

          <input type="hidden" name="source" value="contact">

          <p id="demo-error" class="hidden text-[0.88rem] text-[var(--accent)] font-semibold" role="alert"></p>

          <button type="submit" id="demo-submit" class="btn btn-primary w-full">Book a field visit</button>

          <p class="text-[0.78rem] text-[var(--text-3)] leading-relaxed">
            We use this to reply and to plan the visit. Nothing else, and nobody else.
          </p>
        </div>
      </form>

      <div id="demo-success" class="hidden text-center py-8">
        <p class="text-[1.3rem] font-bold">Booked. We will be in touch.</p>
        <p class="text-[0.95rem] text-[var(--text-2)] leading-relaxed mt-2 max-w-[36ch] mx-auto">
          Someone will call within two working days to agree which block to walk first.
        </p>
        <a href="/field-report" class="btn btn-ghost mt-6">Read a field report meanwhile</a>
      </div>
    </div>
  </div>
</section>

@push('scripts')
<script src="/assets/scripts/demo-request.js" defer></script>
@endpush

@endsection
